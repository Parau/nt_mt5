# Bridge staging (`MQL5/refactoring/bridge`)

**Fase 1:** sem alterações à bridge RPyC. O `mt5_bridge.py` em produção mantém-se como está.

Quando uma nova versão da bridge for necessária, colocar aqui (ex.: `mt5_bridge_v002.py`) e actualizar o checklist em `MQL5/refactoring/README.md`.
